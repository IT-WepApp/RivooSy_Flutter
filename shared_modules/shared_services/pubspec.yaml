name: shared_services
description: الخدمات المشتركة بين التطبيقات.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # اعتمادات على الحزم المشتركة الأخرى في المونوربو:
  shared_models:
    path: ../shared_models
  shared_widgets:
    path: ../shared_widgets

  # رفع إصدار cloud_firestore:
  cloud_firestore: ^5.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
