name: shared_widgets
description: عناصر واجهة مستخدم مشتركة بين التطبيقات.
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_models:
    path: ../shared_models
